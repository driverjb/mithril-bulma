import z from 'zod';
import * as c from './classes/index.js';

/**
 * Type used to control margins
 * @typedef {z.infer<typeof c.Margin>} Margin
 */

/**
 * Type used to control margins
 * @typedef {z.infer<typeof c.Padding>} Padding
 */

/**
 * @typedef {z.infer<typeof c.Display>} Display
 */

/**
 * Type used to control visibility settings
 * @typedef {z.infer<typeof c.Visibility>} Visibility
 */

/**
 * Type used to control float settings
 * @typedef {z.infer<typeof c.Float>} Float
 */

/**
 * Type used to control overflow settings
 * @typedef {z.infer<typeof c.Overflow>} Overflow
 */

/**
 * Type used to control alignment settings
 * @typedef {z.infer<typeof c.Alignment>} Alignment
 */

/**
 * Type used to control responsive settings
 * @typedef {z.infer<typeof c.Responsive>} Responsive
 */

/**
 * Type used to control flex settings
 * @typedef {z.infer<typeof c.Flex>} Flex
 */

/**
 * Type used to control container width settings
 * @typedef {z.infer<typeof c.ContainerWidth>} ContainerWidth
 */

/**
 * Type used to control color of text
 * @typedef {z.infer<typeof c.ColorText>} ColorText
 */

/**
 * Type used to control color of text
 * @typedef {z.infer<typeof c.ColorBackground>} ColorBackground
 */

/**
 * Type used to control color of basic elements like buttons and progress
 * @typedef {z.infer<typeof c.Color>} Color
 */
